generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Advisor {
  id      Int     @id @default(autoincrement())
  name    String?
  auth_id String  @unique
  clubs   Club[]  @relation("AdvisorToClub")
}

model Club {
  id       String     @id @unique
  name     String
  slug     String     @unique
  secret   String
  Liveroom Liveroom[]
  members  Member[]
  projects Project[]
  advisors Advisor[]  @relation("AdvisorToClub")
}

model Member {
  id          Int          @id @default(autoincrement())
  name        String
  password    String
  clubId      String
  avatar      Int
  Club        Club         @relation(fields: [clubId], references: [id])
  submissions Submission[] @relation("MemberToSubmission")
}

model Project {
  id          Int          @id @default(autoincrement())
  title       String
  content     String
  clubId      String
  club        Club         @relation(fields: [clubId], references: [id])
  submissions Submission[]
}

model Submission {
  id        Int      @id @default(autoincrement())
  content   String
  projectId Int?
  Project   Project? @relation(fields: [projectId], references: [id])
  members   Member[] @relation("MemberToSubmission")
}

model Liveroom {
  id     String  @id @unique
  fabric String?
  clubId String?
  Club   Club?   @relation(fields: [clubId], references: [id])
}
